This folder is used for the Flask web server to server image files. Every x seconds each client will send an image here if configured to do so and the image will be displayed by the Flask web server.
